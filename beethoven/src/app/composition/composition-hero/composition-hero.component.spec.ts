/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CompositionHeroComponent } from './composition-hero.component';

describe('Component: CompositionHero', () => {
  it('should create an instance', () => {
    let component = new CompositionHeroComponent();
    expect(component).toBeTruthy();
  });
});
